{
  "name": "snake-game",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "wasm": "wasm-pack build ./snake_game_wasm --target web",
    "dev": "yarn wasm && vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^4.5.4",
    "vite": "^2.9.13",
    "vite-plugin-wasm-pack": "^0.1.12"
  }
}
